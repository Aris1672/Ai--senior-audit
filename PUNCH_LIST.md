# AI Senior Auditor — Punch List

Prioritized by what it blocks. Derived from full source review (see PROJECT_STATUS.md for details on each item).

> **Updated July 3, 2026** — Haiku 4.5 removed from the audit pipeline; `SONNET_MODEL` upgraded to `claude-sonnet-5`. Findings are now extracted via a `record_findings` tool_use call on the same Sonnet request that generates the report, instead of a second Haiku call parsing Sonnet's text. Rationale: real usage data (June 30, 2026) showed Haiku at ~2.6% of daily token cost (~$0.20 of $7.63) — not worth the quality risk the split already caused once (the evidence_status field being silently dropped between Sonnet's reasoning and Haiku's extraction schema, fixed July 1). See items below for what this closes and what it opens.

> **Updated July 4, 2026** — Added the tax-profile app-level gate (legal form/tax regime/VAT status — new required wizard fields, server-validated, DB-persisted) and an audit-purpose prompt gate, in response to the same input data producing different risk-tier conclusions across runs and sometimes skipping straight to analysis without asking anything. Also fixed two communication-style issues (self-introduction greeting, prose-vs-bullet summary) and added real markdown rendering to the chat UI. See `PROJECT_STATUS.md` Session Log for full detail.

> **Updated July 4, 2026 (later same day)** — Scoped two Phase-2-gated future tasks: counterparty ЕГРЮЛ verification, and the privacy pre-processing (PII hash-tokenization) pipeline's concrete mechanism — the latter raises an open decision on whether Phase 2 still needs the GigaChat swap at all. See P2 items below and `PROJECT_STATUS.md`'s Phase 2 section.

> **Updated July 4, 2026 (evening)** — Closed out PDF/image parsing entirely (four sequential bugs: `pdf-parse` API mismatch, nested-worker file-tracing failure, `workerSrc` config across three failed attempts, detached-ArrayBuffer crash) and added a `record_findings` duplicate-call guard. Found two new bugs while reviewing a real downloaded report, both scoped as new P1s below: chat only supports single-file attach, and mid-chat re-analysis duplicates findings instead of updating them.

---

## P0 — Security (fix before any real client data touches this, demo included)

- [ ] **Add auth checks to `/api/data`.** Single dispatcher handles 19 actions including admin-only ones (`admin_stats`, `admin_clients`, `delete_client`) with zero authentication. Anyone who knows an action name can read or write any client's data. **Concrete example (July 4, 2026):** `get_or_create_session` creates an `audit_sessions` row with no tax-profile fields, bypassing the gate added the same day — the chat page no longer calls it, but the action itself is still reachable by anyone who POSTs the action name directly, since there's no auth layer to stop them.
- [ ] **Add admin-role check to `/api/admin/clients` and `/api/admin/pricing`.** No server-side check that the caller is actually an admin — only a client-side React redirect, which doesn't protect the API itself.
- [ ] **Add auth to `/api/report/[id]`.** Publicly downloads full audit report (findings, chat history, company data) for any guessable session UUID, no login required.
- [ ] **Delete `/api/report/[id]` and `/api/report/test`.** Both are orphaned — the real report generator is client-side (`pdfmake` in the audit detail page). Leaving these deployed is unnecessary attack surface for zero benefit.
- [ ] **Add auth to `/api/chat` and `/api/upload`.** Same pattern — currently trust whatever `clientId`/`sessionId` is passed in.

---

## P1 — Blocks the core demo flow from working end-to-end

- [x] ~~New client creation doesn't create a `client_subscriptions` row.~~ **Verified not a blocker (June 2026 test).** A freshly created client with no `client_subscriptions` row was able to complete the full audit flow successfully — price calculation, confirmation, AI chat, and findings all worked. This confirms `/api/billing/check-limit` is **not actually called anywhere in the new-audit wizard** (`calculate-price` → `confirm_audit` is the real path; `check-limit` is skipped entirely). The route and its underlying `get_client_limit()` Postgres function are fully built but currently unused — reclassified below as a P2 cleanup/decision item, not a P1 blocker.
- [ ] **Add `audit_sessions.paid` to a tracked migration.** The column is used everywhere (admin billing log, both dashboards' donut charts) but isn't in `001–004.sql` — it was added outside version control. If this gets lost or a fresh DB is provisioned from migrations alone (e.g. for Phase 2 SpaceWeb setup), all paid/unpaid tracking silently breaks.
- [ ] **Fix multi-sheet XLSX/XLS parsing.** Both parsers read only the first sheet. Confirmed in real client testing: a 1C export with "Без подтверждающих документов" (135 rows, audit-relevant) + "Полный список" (178 rows) only ever surfaces sheet 1 to the AI. This directly affects audit accuracy/completeness on real client files.
- [ ] **Add `.txt` to file input `accept` attributes.** The most fully-built parser (1C Client Bank Exchange, Windows-1251 decoding, transaction-level detail) is unreachable through any of the three upload UIs because the file picker filters it out. Either add `.txt` to the accept lists or relax/remove the filter.
- [x] ~~Add PDF text extraction.~~ **Fixed July 4, 2026 (evening).** Was silently broken by a `pdf-parse` v1-vs-v2 API mismatch, then a nested-`pdfjs-dist`-worker file-tracing failure once that was fixed. `pdf-parse` dependency dropped entirely — now uses the top-level `pdfjs-dist` directly (already used for scanned-PDF rendering). Verified on both a text-layer PDF and a real scanned PDF in a live session.
- [x] ~~Verify the Sonnet 5 tool_use findings extraction end-to-end.~~ **Verified July 4, 2026** — real run confirmed single call, `stop_reason: tool_use` after full report text, findings saved correctly. **Also hardened:** added a `toolCallSeen` guard so a second `record_findings` call in the same turn (across a `max_tokens` continuation) is dropped with a warning instead of silently concatenated — this specific scenario (multi-continuation) still hasn't been exercised by a real long report, so treat the guard itself as reactive-but-unverified.
- [ ] **Add `.jpg`/`.jpeg`/`.png` to the chat page's attachment `accept` attribute.** **Partially fixed July 4, 2026** — images now selectable and processed correctly via vision. Documents page and new-audit wizard not yet re-checked for the same gap.
- [ ] **NEW (July 4, 2026 evening) — Support multiple file attachments in chat.** `handleFileSelect` only reads `e.target.files?.[0]` and the input lacks `multiple`; `uploadAndSend` handles one file per call. User-facing limitation, easy fix (loop + `multiple` attribute), but touches the same send-flow code as the duplicate-findings bug below — consider fixing together.
- [ ] **NEW (July 4, 2026 evening) — Mid-chat re-analysis duplicates findings instead of updating them.** Confirmed via a real downloaded report: multiple violations appeared 2-3x, near-verbatim, as separate findings (e.g. missing-documents finding repeated as #1/#4/#7). Each document upload mid-chat appears to trigger a full fresh `record_findings` call with no awareness of findings already saved earlier in the same session. Two possible fix angles, likely need both: (a) prompt-level — tell Sonnet what's already been found (session's existing findings) and instruct it to report only *new or changed* findings each turn; (b) app-level — `saveFindings()` needs a dedup/upsert check against existing rows (by session + similarity/title match) rather than blind insert. Investigate before deciding the fix shape.
- [ ] **Verify the audit-purpose prompt gate fires reliably across repeated runs.** (New, July 4 2026.) `AUDIT_SYSTEM_PROMPT` now instructs the model to ask about audit purpose before any analysis, if not already stated — but this is prompt-only, not app-enforced, unlike legal_form/tax_regime/vat_status which were moved to a hard gate specifically because prompt-only enforcement was already proven unreliable (one of three identical test runs skipped straight to analysis with no questions at all). Confirmed working in one real screenshot review, but "worked once" isn't the same bar as the tool_use item above. Run several fresh sessions and confirm the purpose question fires every time before deciding this is solid, or reconsider making it a 4th app-level gate (session already has a place to store it — `audit_sessions` — if the decision changes).

---

## P2 — Correctness issues that produce misleading output, not hard failures

- [ ] **Reconcile `types/index.ts` with the real schema.** Risk levels, account status, and session status enums are all wrong in this file (English placeholders vs. the actual Russian-string enums in the SQL). Low runtime risk since most routes define inline types instead of importing this file, but it actively misleads anyone (including future-you, or another AI session) who trusts it.
- [ ] **Stop encoding company name/period into `audit_sessions.title` + regex-parsing it back out.** Breaks silently if a company name contains `(`. The schema already has unused `period_from`/`period_to` columns — use them, and add a `company_name` column or pull from `profiles.company_name` instead.
- [ ] **Fix the `paid` status check inconsistency in `/api/auth/login`.** Login blocks `status === 'paused'` but not `'deleted'`, while `check-limit` blocks both. A soft-deleted client could potentially still log in.
- [ ] **Decide on one data-access convention.** Right now `/api/data`'s action dispatcher and dedicated REST routes (`/api/admin/clients`, `/api/admin/pricing`) coexist for overlapping concerns. Pick one pattern going forward to avoid the inconsistency compounding as features are added.
- [ ] **Decide the fate of `/api/billing/check-limit` and `client_subscriptions`.** Confirmed unused — the new-audit wizard never calls it, so audits/pricing have no actual cap right now regardless of tier or subscription state. Either wire it into the wizard (if usage limits matter for the business model) or remove it/document it as aspirational, so it doesn't mislead anyone reading the code into thinking limits are enforced.
- [ ] **Reconcile token pricing constants.** (Updated July 3 2026.) `lib/anthropic.ts` no longer defines Haiku pricing at all — `HAIKU_PRICING` was removed along with `HAIKU_MODEL` when Haiku was dropped from the audit pipeline. If `lib/billing.ts` or the client usage page (`UsageBreakdown` type) still reference Haiku rates or a per-model cost split, those are now dead/misleading and need to be updated to reflect a Sonnet-only pipeline. Also: `SONNET_PRICING` in `lib/anthropic.ts` is still the standard $3/$15-per-1M rate, but Sonnet 5 launched at **introductory pricing of $2/$10 per 1M through August 31, 2026** — so client-facing cost estimates are currently ~50% too high until either the constant is updated or the intro period ends. Fix both in the same pass.
- [ ] **Counterparty ЕГРЮЛ / shell-company verification.** (New, July 4 2026.) The AI can currently only recommend a human check the registry for a suspicious counterparty — it can't check itself. Scoped as a `check_counterparty` tool_use call (same pattern as `record_findings`), but **blocked on the Phase 2 SpaceWeb migration** — calling a Russian data provider from Vercel (outside Russia) risks the same class of geo/reachability problem this whole architecture already works around in the opposite direction, unverified either way. Full data-source/vendor tradeoffs (free ЕГРЮЛ basics vs. paid shell-company scoring vs. ГИР БО for assets) in `PROJECT_STATUS.md`'s P2 section.
- [ ] **Privacy pre-processing pipeline — mechanism now concrete, one open strategic decision.** (Updated July 4 2026.) Russian LLM detects PII spans (ФИО, phone, address, individual INN) → deterministic script replaces each with a consistent hash token (`User_A123`) → Sonnet only ever sees tokenized data → script reverses on the response. **Open decision, not yet made:** if this alone satisfies 242-FZ, Phase 2 may not need to replace Sonnet with GigaChat at all for reasoning — worth resolving deliberately before Phase 2 execution starts (currently the GigaChat swap is the default plan by inertia, not by decision). See `PROJECT_STATUS.md` Phase 2 section.

---

## P3 — Cleanup (no functional impact, reduces future confusion/bundle size)

- [ ] Remove unused dependencies: `pdf-parse` (until wired up per P1), `papaparse` (CSV is hand-rolled), `chart.js` (all charts are hand-rolled SVG/canvas).
- [ ] Consolidate the three independent donut chart implementations (admin dashboard SVG, client dashboard canvas, audit detail canvas) into one shared component.
- [ ] Remove or document `proxy.ts` — it's currently a no-op middleware; either give it a real purpose (e.g. server-side auth gating, which would also help with P0) or delete it so it doesn't imply protection that isn't happening.
- [ ] Wire `audit_messages.tokens_in`/`tokens_out` and `usage_events` AI-message logging — schema supports per-message token/cost tracking but the chat route never writes it, so the `UsageBreakdown` type in `billing.ts` and the client usage page can't show real cost data. (Simpler to wire up now that each chat turn is a single Sonnet call rather than a Sonnet+Haiku pair — one `usage` object per turn instead of two to reconcile.)
- [ ] Decide the fate of the `transactions` table — either start populating it for structured per-transaction analysis (likely needed eventually for risk_score work) or remove it from the schema if the flat-text-to-LLM approach is the permanent design.
- [ ] Remove the unused `increment_session_cost()` Postgres function if nothing will call it.

---

## Suggested order of attack

1. P0 security items — these are the difference between "demo" and "incident," especially with real client financial data
2. Subscription creation + `paid` migration (P1) — both are quick fixes that unblock the actual sales demo flow
3. Multi-sheet parser fix + `.txt` accept fix (P1) — directly affects whether the AI sees complete data, which is the whole value proposition
4. Everything else as time allows before Phase 2 (SpaceWeb/GigaChat) migration begins, since several P1/P2 items are also called out in the Phase 2 migration checklist and are easier to fix once than to fix twice across two stacks
